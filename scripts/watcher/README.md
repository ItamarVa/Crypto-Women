# Blog sync — Google Drive (ענן) ↔ אתר

המערכת מסנכרנת את **תיקיית ה-Google Drive** `Blog - Crypto Women` עם הבלוג באתר.
**תיקיית ה-Drive היא מקור-האמת.** מאז 2026-07-05 המערכת מושכת **ישירות מהענן** דרך
Drive API (service account) — **בלי תלות ב-Google Drive for Desktop ובלי לוגין**.
זה פותר את החולשה: גם אם המחשב עולה ואף אחד לא מתחבר, המאמרים ממשיכים להתפרסם.

מנגנון: `blog-import.mjs --drive` מושך את רשימת הקבצים מהתיקייה בענן **כל 5 דקות**,
מזהה חדש/שונה/נמחק לפי `modifiedTime`, וממיר+מפרסם.

## מה מסתנכרן אוטומטית

| ב-Drive | באתר |
|---------|------|
| מסמך **חדש** (Google Doc / Word / PDF / MD / TXT…) בתת-תיקיית קטגוריה | נוצר מאמר חדש (תוך ≤5 דק') |
| מסמך קיים **שהשתנה** (גם עריכה אונליין ב-Docs) | המאמר מתעדכן (+ תאריך עדכון) |
| מסמך **שנמחק** מהתיקייה | המאמר יורד מהאתר (אחרי גיבוי) |

הפרסום אוטומטי: משיכה מהענן → המרה → commit → push → בנייה → אונליין.
- **Google Docs** מיוצאים ל-Markdown ישירות ע"י Drive (בלי markitdown).
- **Word/PDF** מורדים מהענן וממירים עם markitdown (venv על D:).

## הקבצים

| קובץ | מה עושה |
|------|---------|
| **install-autostart.bat** | מתקין הפעלה אוטומטית **בכל הדלקת מחשב, ללא לוגין**. הרץ **פעם אחת** (יבקש הרשאת מנהל — **בלי סיסמה**, logon מסוג S4U). מפעיל מיד. |
| **uninstall-autostart.bat** | עוצר ומסיר את ההפעלה האוטומטית. |
| **start-watcher.bat** | מפעיל ידנית בחלון (לוגים חיים). שימושי לבדיקה. |
| **stop-watcher.bat** | עוצר watcher שרץ. לא מסיר את ההפעלה האוטומטית. |

## פרוצדורות (הכל ב-Google Drive)

- **מאמר חדש:** צור Google Doc (או העלה Word/PDF) בתוך תת-תיקיית הקטגוריה ב-Drive. תוך ≤5 דק' הוא עולה.
- **עריכת מאמר:** ערוך את המסמך ב-Drive (כולל עריכה אונליין ב-Google Docs). תאריך הפרסום **לא משתנה** — נשאר תאריך היצירה; רק "תאריך עדכון" מתעדכן.
- **הסרת מאמר:** מחק את המסמך מהתיקייה ב-Drive. המאמר יורד מהאתר. עותק נשמר בגיבוי.
- **שינוי קטגוריה:** העבר את המסמך לתת-תיקיית קטגוריה אחרת ב-Drive.
- **דריסת תאריך פרסום (חריג):** ראה למטה.

## קטגוריות = תת-תיקיות ב-Drive
שמור בתת-תיקייה בשם מדויק: `מיינדסט` · `השקעות` · `ביטקוין` · `בלוקצ'יין` · `מדריך` · `כללי`.
מסמך בשורש התיקייה → קטגוריה `כללי`.

## דריסת תאריך פרסום — `blog-dates.csv`
בשורש התיקייה יש קובץ **`blog-dates.csv`** שהמערכת מזינה אוטומטית (שורה לכל מאמר).
פתח אותו ב-Google Sheets / Excel וערוך **רק** את עמודת `דריסת_תאריך`:
- ריק = תאריך הפרסום המקורי (תאריך יצירת המסמך).
- מלא (`YYYY-MM-DD` או `DD/MM/YYYY`) = דורס את תאריך הפרסום של המאמר.
- מחיקת הערך = חזרה לתאריך המקורי.
אין לערוך את עמודת `מזהה`. השינוי מוחל אוטומטית תוך שניות.

## גיבוי
- **לוקלי:** `D:\AI Projects\Crypto Women\Blog Backup - Crypto Women` — זמין תמיד (ללא לוגין),
  עותק של **כל** מאמר שפורסם אי-פעם כולל מאמרים שהוסרו. לעולם לא נמחק. (`BLOG_BACKUP` לשינוי נתיב.)
- **מחוץ למחשב:** ה-repo ב-**GitHub** — כל פוסט מקומוט עם היסטוריה מלאה, אז כל מאמר (כולל מחוקים)
  ניתן לשחזור מ-git. זהו הגיבוי העמיד-לתקלת-דיסק, ואינו תלוי בלוגין.

## הפעלה אוטומטית אחרי ריסטרט (ללא לוגין) — עכשיו באמת עצמאי
ה-task רץ **בכל הדלקה, לפני/בלי לוגין** (S4U, AtStartup, StartWhenAvailable, RestartCount 3),
ומושך את התוכן **ישירות מהענן** דרך Drive API. לכן **אין יותר תלות בלוגין או ב-Drive-for-Desktop**:
גם אם אף אחד לא מתחבר במשך שבועות, מאמרים חדשים/מעודכנים ב-Drive ממשיכים להתפרסם כל 5 דק'.
(מצב ה-`--watch` הישן, שצפה בתיקייה לוקלית ותלוי בלוגין, נשאר בקוד לבדיקות בלבד.)

## בטיחות מפני מחיקה בטעות
כדי שתיקייה לא-מסונכרנת לא תמחק את הבלוג: סנכרון-מחיקה נעצר אם התיקייה חסרה/ריקה,
אם יש בה הרבה פחות מסמכים מהצפוי (Drive באמצע סנכרון), או אם נדרשות יותר מ-5 מחיקות
בבת אחת (משתנה `BLOG_MAX_DELETE`). במקרים כאלה נרשמת אזהרה ל-`watcher.log` ואין מחיקה.

## משיכה מהענן (Drive API) — הפרטים
- מודול `drive-client.mjs` מדבר עם Drive API (service account, scope `drive.readonly`).
- מזהה תיקיית ה-Drive: `BLOG_DRIVE_FOLDER_ID` (ברירת מחדל: `1jg4ie1Cae6tRqPughuD6FKOiOCeLGL9x`).
- **מיגרציה חד-פעמית (כבר בוצעה):** `node scripts/blog-import.mjs --drive-seed` מיפה את 29 הפוסטים הקיימים ל-Drive-ledger בלי לשכתב (אפס churn), כולל מיפוי ה-Google Doc ל-slug הקיים.
- Ledger נפרד: `scripts/.blog-drive-ledger.json` (gitignored, ממופתח לפי Drive file id).
- אם משיכת הרשימה מהענן נכשלת (רשת/API) — הסבב מדולג (לא נמחק כלום — בטיחות).

## Google Docs (`.gdoc`) — פרסום אוטומטי
קובץ `.gdoc` בתיקייה הוא רק **מצביע** ל-Google Docs (JSON, בלי תוכן). כשמוגדר
מפתח **service-account** של Google Drive, ה-watcher מייצא את המסמך האמיתי ישירות
ל-Markdown דרך Drive API (בלי markitdown) ומפרסם אותו כמו כל מסמך — בלי ייצוא ידני.

**עריכה אונליין:** עריכת המסמך ב-Google Docs לא נוגעת בקובץ המקומי, לכן ה-watcher
בודק את Drive כל 10 דקות (`BLOG_GDOC_POLL_MS`) ומעדכן מאמרים ששונו אונליין.
**תאריך פרסום** נלקח מתאריך היצירה ב-Drive (מדויק יותר מתאריך הסנכרון המקומי).

### התקנה חד-פעמית (פעם אחת בלבד)
1. [Google Cloud Console](https://console.cloud.google.com/) → צור/בחר פרויקט → הפעל **Google Drive API**.
2. **IAM & Admin → Service Accounts** → צור service account → **Keys → Add key → JSON** → הורד.
3. שמור את קובץ ה-JSON כ-**`scripts/watcher/gdrive-sa.json`** (כבר ב-`.gitignore` — סוד, לא נדחף).
4. העתק את כתובת האימייל של ה-service account (למשל `xxx@project.iam.gserviceaccount.com`).
5. ב-Google Drive של קרן: **שתף** את התיקייה `Blog - Crypto Women` עם אותו אימייל (הרשאת **Viewer**). זה מספיק — כל מסמך בתת-התיקיות יורש את השיתוף.

זהו. משתנה סביבה `GOOGLE_SA_KEY` יכול להצביע על נתיב אחר במקום ברירת המחדל.
בלי המפתח — קבצי `.gdoc` פשוט מדולגים עם אזהרה ל-`watcher.log` (שאר הסנכרון תקין).

## דרישות (מותקנות במחשב זה)
`node`, `git`.
- **markitdown ל-Word/PDF:** מותקן ב-venv על **D:** — `D:\AI Projects\Crypto Women\tools\markitdown-venv` — ו-`start-watcher.bat` מצביע אליו עם `MARKITDOWN_BIN`. ⚠️ **חובה על D: (או נתיב אחר שה-task רואה), לא ב-`AppData\Roaming`:** תחת S4U ה-roaming profile לא נטען, אז markitdown שהותקן ב-`uv tool` לא נגיש ל-task. לשחזור: `<Local-Python>\python.exe -m venv "D:\...\tools\markitdown-venv"` ואז `...\Scripts\python.exe -m pip install "markitdown[all]"`.
- **ל-`.gdoc`:** `google-auth-library` (מותקנת) + `gdrive-sa.json` (ראה למעלה). Google Docs לא צריכים markitdown כלל (ייצוא Markdown ישיר מ-Drive).

## הערות
- `.gsheet` / `.gslides` (Sheets/Slides) מדולגים — אינם מאמרי בלוג.
- לוג הרצה: `watcher.log` (בתיקייה זו).
- לעצירה זמנית של הפרסום האוטומטי: `stop-watcher.bat`.
